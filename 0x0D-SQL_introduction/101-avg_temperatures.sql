-- This script that displays the average temperature (Fahrenheit)
-- by city ordered by temperature (descending)
SELECT city, AVG(value) AS avg_temp FROM  AVG(value) AS avg_temp FROM temperatures
GROUP BY city
ORDER BY value DESC temperatures
GROUP BY city
ORDER BY value DESC
